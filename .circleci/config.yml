version: 2.1

workflow:
  test-deploy:
    - build

jobs:
  build:
    docker:
      - image: cimg/base:2021.10
    steps:
      - checkout
      - run: |
          docker build -f Dockerfile -t yahav876/tikal-yahav:1.0.0 .
      